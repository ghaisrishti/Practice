name: My First Workflow
on: push
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: print message
        run: echo "Hello, GitHub Actions!"
      - name: print name
        run: echo "hello, srishti here!" 
